<h1>movie details</h1>
<h3>{{movie.title}}</h3>
Genre: <h3>{{movie.genre}}</h3>
Plot: <p>{{movie.plot}}</p>
Cast:
{{#each movie.cast}}
    <p>{{name}}, {{occupation}}, {{catchphrase}}</p>
{{/each}}
<form action="/movies/{{movie._id}}/delete" method="get">
    <button type="submit">Delete</button>
</form>